package polymorphism;

public interface Interface1 {
	//interface is an alternate for abstract class. And it helps to make all methods in class also abstract.

	String getName();
	
	/* iff there are inheritance between 2 classes then"extends" is used.
	 * if there is inheritance of a interface in a class then "implements" is used
	 * if there are inheritance between 2 interface then"extends" is used.
	 */

}
